import test from 'ava'
import 'babel-core/register'
const exec = require('child_process').exec

test.before(t => {
  // this runs before all tests
  // const cmd = 'node launch-server.js --port '
  // let child = exec(cmd)
})


test.cb('render server pipeline', t => {
})
